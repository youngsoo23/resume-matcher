# resume-matcher 기획문서

> 이 문서는 프로젝트 초기 기획 스냅샷이다. 현재 구현 상태를 반영해 보강한 상세 문서 패키지는
> [docs/planning/](docs/planning/00-index.md)에 있다.

## 한 줄 소개
내 이력서와 채용공고(JD)를 넣으면, AI와 대화하며 그 공고에 맞게 이력서를 다듬어주는 개인용 툴.

## 사용자 플로우
1. **이력서 등록**: 처음 접속 시 이력서 원문(텍스트/마크다운)을 붙여넣어 저장한다.
2. **JD 입력**: 지원하려는 채용공고 JD 텍스트를 붙여넣는다.
3. **AI와 대화**: 채팅창에서 "이 JD에 맞게 강점을 더 부각시켜줘" 같은 요청을 하면, AI가 대화 맥락(이력서+JD+대화기록)을 바탕으로 응답하고, 필요 시 **수정된 이력서 전문**을 함께 제시한다.
4. **적용/버전 관리**: AI가 제안한 수정본을 "적용" 버튼으로 현재 이력서로 교체할 수 있고, 이전 버전은 목록에서 확인·복원 가능하다.
5. **내보내기**: 최종본을 복사하거나 텍스트로 다운로드한다.

## 아키텍처
```
[브라우저] (localStorage)
  - 원본 이력서
  - 이력서 버전 히스토리
  - JD
  - 채팅 대화 기록
        │ fetch (JSON)
        ▼
[Spring Boot 백엔드] (Kotlin, DB 없음)
  - POST /api/chat
    → Claude API 호출 (ANTHROPIC_API_KEY는 서버 환경변수, 프론트에 노출 안 됨)
    → 응답 텍스트 + (있다면) 수정된 이력서 전문을 파싱해 반환
        │
        ▼
[Anthropic Claude API]
```

- **데이터 저장**: 전부 브라우저 localStorage. 서버는 상태를 갖지 않는 프록시 역할만 함 (DB 없음).
- **인증**: 없음 (개인용 단일 사용자 로컬 툴).
- **프론트엔드**: 별도 빌드 도구 없이 순수 HTML/CSS/JS 한 페이지. `src/main/resources/static/`에 두고 Spring Boot가 정적 파일로 서빙.
- **백엔드**: 기존 Spring Boot WebMVC 스켈레톤에 `/api/chat` 컨트롤러만 추가. Anthropic Messages API를 `RestClient`로 직접 호출.

## API 설계
### `POST /api/chat`
**Request**
```json
{
  "resume": "현재 이력서 전문",
  "jobDescription": "JD 전문",
  "messages": [
    {"role": "user", "content": "이전 사용자 메시지"},
    {"role": "assistant", "content": "이전 AI 응답"}
  ]
}
```
**Response**
```json
{
  "reply": "AI의 대화형 응답 (설명/제안 코멘트)",
  "updatedResume": "수정된 이력서 전문 (수정 제안이 없으면 null)"
}
```

### 시스템 프롬프트 방향
"당신은 이력서 코치입니다. 주어진 이력서와 JD, 대화 맥락을 참고해 조언하고, 이력서를 수정할 때는 항상 전체 수정본을 반환하세요." + 응답 포맷(설명/수정본 분리)을 강제하는 지시.

## MVP 범위 (이번에 구현)
- [ ] 백엔드 `/api/chat` 엔드포인트 (Claude API 연동)
- [ ] 프론트 한 페이지: 이력서 입력창, JD 입력창, 채팅 패널, "적용" 버튼
- [ ] localStorage 기반 이력서 버전 히스토리 (목록 + 복원)
- [ ] 최종 이력서 복사/다운로드

## 범위 밖 (MVP 이후)
- 다중 사용자/로그인
- PDF 등 파일 업로드·파싱 (초기엔 텍스트 붙여넣기만)
- 여러 채용공고 동시 관리
- 배포/CI
